% better looking tables
\usepackage{booktabs}

% direct input of UTF-8 characters
\usepackage[utf8]{inputenc}

% row spacing
\usepackage{setspace}
% \onehalfspacing
\doublespacing

% fixing long titles "overflowing" the page
\usepackage{fancyhdr}

% fixing too many consecutive floats (e.g. all the tables and plots)
\maxdeadcycles=1000
